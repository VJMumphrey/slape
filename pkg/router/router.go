/*
Package router implements a standard semantic router usefull in the deployment of ai applications.

Semantic routers are useful in determining the reason or message of a prompt.
This is good for something like security, or censoring information like political views.

At some point this may get moved to its own repo, but for now its here.
*/
package router

type (
	SemanticRouter struct {
	}
)
